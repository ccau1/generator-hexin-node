{
	"title": "<%= name %>",
	"baseUrl": "/api",
	"port": 8280,
	"debug": true,
  "secret": "1232412421412",
  "host": "http://localhost",
	"mail": {
		"service": "<%= mailerService || '' %>",
		"auth": {
			"user": "<%= mailerAuthUser || '' %>",
			"pass": "<%= mailerAuthPass || '' %>"
		}
	},
	"redis": {
		"host": "<%= redisHost || '' %>",
		"port": "<%= redisPort || '' %>"
	},
	"fivebeans": {
		"host": "<%= fivebeansHost || '' %>",
		"port": "<%= fivebeansPort || '' %>"
	},
	"db": {
		"main-db": {
			"engine": "mongo",
			"connection": {
				"hosts": ["localhost:27017"],
				"database": "<%= name %>"
			},
			"default": true
    }
	}
}
